export const metadata = {
  title: 'Order Lookup | Home Grown Money',
  description: 'Check your Home Grown Money order status using your order number and email address.',
  alternates: {
    canonical: '/order-lookup',
  },
};

export default function OrderLookupLayout({ children }) {
  return children;
}
